# 在 v0.app 上使用本项目

## 🚀 快速开始

本项目可以在 v0.app 上正常运行，Supabase 是**可选的**功能。

### 选项 1：不使用 Supabase（推荐用于 v0.app）

直接在 v0.app 上编辑和部署，无需任何额外配置。应用会自动检测 Supabase 是否启用。

**步骤：**
1. 在 v0.app 上导入或创建项目
2. 不设置任何 Supabase 相关的环境变量
3. 应用会跳过所有 Supabase 功能，正常运行

### 选项 2：在 v0.app 上使用 Supabase

如果你想在 v0.app 上使用 Supabase，需要使用 Supabase 云服务（而不是本地实例）。

**步骤：**

#### 1. 创建 Supabase 云项目

访问 [https://supabase.com](https://supabase.com)
- 注册/登录账号
- 创建新项目
- 等待项目初始化完成

#### 2. 获取 API 凭证

在 Supabase Dashboard：
1. 进入你的项目
2. 点击左侧菜单 **Settings** → **API**
3. 复制以下信息：
   - **Project URL**（项目 URL）
   - **anon/public key**（公开密钥）

#### 3. 在 v0.app 设置环境变量

在 v0.app 项目设置中添加环境变量：

```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

**重要提示：**
- 使用 `https://` 开头的云服务 URL
- 不要使用 `http://127.0.0.1:54321`（这是本地开发地址）

#### 4. 重新部署

在 v0.app 上保存并重新部署项目，Supabase 功能将自动启用。

---

## 🔧 常见问题

### Q: v0.app 上提示 Supabase 登录

**A:** 这是因为项目依赖中包含了 `@supabase/supabase-js`，但你不需要登录 Supabase。解决方法：

1. **不使用 Supabase**：忽略这个提示，应用仍然可以正常运行
2. **使用 Supabase 云服务**：按照上面"选项 2"的步骤配置

### Q: 如何完全移除 Supabase

如果你想完全移除 Supabase 依赖：

1. 编辑 `package.json`，删除以下依赖：
```json
"@supabase/supabase-js": "...",
"@supabase/ssr": "..."
```

2. 删除或重命名以下文件夹：
```
lib/supabase/
middleware.ts
```

3. 重新部署

### Q: 本地开发 vs v0.app 的区别

| 功能 | 本地开发 | v0.app |
|------|----------|--------|
| Supabase 本地实例 | ✅ 支持 | ❌ 不支持 |
| Supabase 云服务 | ✅ 支持 | ✅ 支持 |
| 无 Supabase 运行 | ✅ 支持 | ✅ 支持 |

---

## 📝 环境变量配置参考

### 不使用 Supabase
```bash
# 不设置任何变量，或者：
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

### 使用 Supabase 云服务
```bash
NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 本地开发使用 Supabase
```bash
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
```

---

## 🎯 推荐工作流程

### 方案 A：混合开发（推荐）

1. **本地开发**：使用本地 Supabase 实例
2. **v0.app 编辑**：禁用 Supabase，专注于 UI/UX
3. **生产部署**：使用 Supabase 云服务

### 方案 B：纯云开发

1. 创建 Supabase 云项目
2. 本地和 v0.app 都使用相同的云服务
3. 统一的开发和部署环境

---

## 🔗 相关链接

- [Supabase 官网](https://supabase.com)
- [Supabase 文档](https://supabase.com/docs)
- [v0.app 环境变量设置](https://v0.dev/docs/environment-variables)

---

## ✅ 检查清单

使用此清单确保项目在 v0.app 上正常运行：

- [ ] 项目可以在本地正常启动
- [ ] 决定是否使用 Supabase
  - [ ] 不使用：无需任何配置
  - [ ] 使用：已创建 Supabase 云项目并获取凭证
- [ ] 在 v0.app 上设置了正确的环境变量（如果使用 Supabase）
- [ ] 在 v0.app 上测试部署
- [ ] 功能正常工作

---

**💡 提示：** 如果你只是想在 v0.app 上编辑 UI 和基础功能，完全不需要配置 Supabase。应用会自动适配！

